import 'package:flutter/material.dart';

/// Placeholder. Fuera del alcance de la Parte 2; se completaría junto con
/// autenticación/permisos (Parte 6).
class PerfilScreen extends StatelessWidget {
  const PerfilScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Perfil')),
      body: const Center(
        child: Text('Pantalla de perfil (pendiente de autenticación).'),
      ),
    );
  }
}
